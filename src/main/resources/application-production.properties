server.port=${PORT:0}
spring.application.name=users-ws
eureka.client.serviceUrl.defaultZone=http://localhost:8010/eureka
spring.devtools.restart.enabled=true

gateway.ip=localhost
token.expiration_time=864000000
token.secret=sade45a48eeff4

#will not work without spring data jpa
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.h2.console.enabled=true
#spring.h2.console.settings.web-allow-others=true

spring.datasource.url=jdbc:postgresql://localhost:5432/photo_app?serverTimezone=GMT
spring.datasource.username=postgres
spring.datasource.password=welcome1

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# set application host
server.address=localhost

#configuration to set the ip of host instead of the default localhost
eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=${server.address}
#configuration to set the instance id for multiple instances
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

management.endpoints.web.exposure.include=mappings

logging.level.com.example.photoapp.api.users.data.AlbumsServiceClient=DEBUG
feign.circuitbreaker.enabled=true

spring.zipkin.service.name=users-ws
spring.zipkin.base-url=http://localhost:9411/
spring.zipkin.sender.type=web
spring.sleuth.sampler.probability=1


logging.file.name=users-ws.log

myapp.environment=production